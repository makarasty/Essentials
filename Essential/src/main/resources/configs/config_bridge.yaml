# Bridge server address (for client mode connection)
address: "127.0.0.1"
# Bridge server port number
port: 10371
# Settings for sharing data with bridge server
sharing:
  # Share ban list with bridge server
  ban: false
  # Share broadcast messages with bridge server
  broadcast: false
